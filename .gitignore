# Smartsheet templates are fetched locally via ./download_templates.sh,
# not committed (free to use, not to redistribute).
smartsheet-*.xlsx
*.xlsx.invalid

# Local-only working material (workshop notes, scorecards, test runs)
.private/

# macOS
.DS_Store
